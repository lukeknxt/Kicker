QUnit.test("Angle test", function( assert ) {
  assert.equal(isAngle(60), 60, "Angle is tested");
});

QUnit.test("Radius test", function( assert ) {
  assert.equal(isRadius(60), 60, "Radius is tested");
});